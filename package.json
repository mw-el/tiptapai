{
  "name": "tiptapai",
  "version": "0.1.0",
  "description": "Minimalistischer Desktop Markdown-Editor mit WYSIWYG (TipTap)",
  "main": "main.js",
  "scripts": {
    "build": "esbuild renderer/app.js --bundle --outfile=renderer/app.bundle.js --format=iife",
    "watch": "esbuild renderer/app.js --bundle --outfile=renderer/app.bundle.js --format=iife --watch",
    "start": "npm run build && NODE_ENV=production env -u ELECTRON_RUN_AS_NODE npx electron .",
    "start:desktop": "npm run build && NODE_ENV=production env -u ELECTRON_RUN_AS_NODE npx electron . --no-sandbox",
    "dev": "npm run build && env -u ELECTRON_RUN_AS_NODE npx electron . --inspect=5858"
  },
  "keywords": [
    "markdown",
    "editor",
    "tiptap",
    "wysiwyg",
    "electron"
  ],
  "author": "Matthias",
  "license": "MIT",
  "dependencies": {
    "@tiptap/core": "^3.20.0",
    "@tiptap/extension-image": "^3.22.3",
    "@tiptap/extension-table": "^3.20.0",
    "@tiptap/extension-table-cell": "^3.20.0",
    "@tiptap/extension-table-header": "^3.20.0",
    "@tiptap/extension-table-row": "^3.20.0",
    "@tiptap/markdown": "^3.20.0",
    "@tiptap/starter-kit": "^3.20.0",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "js-yaml": "^4.1.0",
    "jszip": "^3.10.1",
    "node-pty": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^40.6.1",
    "electron-builder": "^26.8.1",
    "electron-reload": "^2.0.0-alpha.1",
    "esbuild": "^0.27.3"
  }
}
